name: easy_server_cli
description: The cli for the easy server tool.
version: 1.0.4
repository: https://github.com/Yoeri-z/EasyServer

executables:
  easyserver: easy_server_cli

environment:
  sdk: ^3.0.3

# Add regular dependencies here.
dependencies:
  yaml: ^3.1.2
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
